name: Build anti-evil-maid

on:
  push:
    branches:
      - main
    paths:
      - 'security-tools/anti-evil-maid/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build Release
        run: cargo build --release
        working-directory: security-tools/anti-evil-maid
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: anti-evil-maid-binary
          path: security-tools/anti-evil-maid/target/release/anti-evil-maid
